all:
  hosts:
    aws:
      ansible_ssh_host: 67.202.9.255
      # ansible_ssh_common_args: -i ./keys/aws-key.pem -o StrictHostKeyChecking=no
      ansible_user: ubuntu
      ansible_python_interpreter: /usr/bin/python3
